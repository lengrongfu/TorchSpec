# Configuration for P-EAGLE training on Qwen3-8B.

model:
  target_model_path: Qwen/Qwen3-8B
  trust_remote_code: true
  draft_model_config: configs/draft_models/qwen3_8b_p_eagle.json

dataset:
  train_data_path: examples/data/sample_conversations.jsonl
  chat_template: qwen
  prompt_key: conversations

training:
  draft_training_mode: p_eagle
  p_eagle_depth: 7
  p_eagle_mask_token_id: 151643
  p_eagle_chunk_size: 0
  attention_backend: flex_attention
  micro_batch_size: 1
  draft_accumulation_steps: 1
  learning_rate: 1e-4
  max_concurrent_batches: 1
  max_grad_norm: 0.5
  max_seq_length: 16384
  num_epochs: 1
  seed: 42
  training_num_gpus_per_node: 2
  training_num_nodes: 1
  ttt_length: 7
  save_per_epoch: true
  warmup_ratio: 0.015

inference:
  inference_engine_type: sgl
  inference_num_gpus: 1
  inference_num_gpus_per_engine: 1
  inference_num_gpus_per_node: 1
  max_sample_pool_size: 64
  inference_buffer_threshold: 32
  inference_batch_size: 8
  sglang:
    tp_size: 1
    mem_fraction_static: 0.7

mooncake:
  master_server_address: null
  metadata_server: null
  protocol: tcp
  global_segment_size: 16GB
  local_buffer_size: 4GB

output_dir: ./outputs/qwen3-8b-p-eagle
cache_dir: ./cache/qwen3-8b-p-eagle
model_download_dir: null

debug:
  save_debug_train_data: null
  debug_train_only: false
  debug_inference_only: false
